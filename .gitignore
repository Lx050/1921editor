# Dependencies
node_modules/

# Build outputs
dist/
dist-ssr/
build/
out/
temp/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment variables (never commit secrets)
.env
.env.local
.env.*.local
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS generated files
.DS_Store
Thumbs.db
._*
.Spotlight-V100
.Trashes

# Debug artifacts (never commit)
*_debug.json
*_debug.txt
debug_*.json
debug_*.txt
build_error*.txt
migration_error*.txt
*_log.txt
schema_audit.txt

# Temporary / backup source files
*.tmp
*.bak
*.orig

# User uploads (store externally, not in git)
uploads/
content-backend/uploads/

# Binary archives (use releases, not git)
*.tar.gz
*.zip
*.rar
*.7z

# Test fixtures (large binaries)
test/*.docx
test/*.zip

# Lighthouse reports (keep only latest in docs/)
lighthouse-reports/

# Deployment config
config/

# SSH/SSL private keys (NEVER commit)
*.pem
*.key

# Exclude sensitive documentation and initialization scripts from public repo
# Note: These are now cleaned but the user asked to exclude them
公众号图文上传全流程接口文档.md
content-backend/src/init-tenant-config.ts
